import React from "react"
import { Link } from "gatsby"; // IF we want to add links on the site we use this instead of a tags ex. <Link to="/">Home</Link>
import Layout from '../components/layout';

export default () => (
    <Layout>
        <div>Hello world!</div>
    </Layout>
);
